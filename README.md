# 🎵 Music Streaming Telegram Mini App

Музыкальный стриминг-сервис в формате Telegram Mini App с системой подписок, покупки треков и интеграцией ЮKassa.

## 📋 Возможности

### Для пользователей:
- 🎧 Каталог треков с поиском и фильтрацией по категориям
- 🆓 1 бесплатное прослушивание каждого трека в день
- ⭐ Безлимитное прослушивание с подпиской
- 💳 Покупка отдельных треков
- 📱 Мини-плеер с перемоткой и регулировкой громкости
- 👤 Профиль с историей прослушиваний и покупок

### Для администратора:
- 📊 Дашборд со статистикой и графиками
- 🎵 Управление треками (загрузка, редактирование, удаление)
- 📁 Управление категориями
- 👥 Просмотр пользователей и их активности
- 💰 Список всех платежей
- ⚙️ Настройка цен и лимитов

## 🛠 Технологии

### Frontend:
- React 18 + TypeScript
- Vite
- TailwindCSS
- TanStack Query (кэширование)
- Zustand (состояние)
- Howler.js (аудиоплеер)
- Chart.js (графики)

### Backend:
- Vercel Serverless Functions
- Prisma ORM
- Supabase PostgreSQL
- Supabase Storage

### Интеграции:
- Telegram Web App SDK
- ЮKassa для платежей

---

## 🚀 Инструкция по деплою

### Шаг 1: Подготовка Supabase

1. **Создайте проект на [supabase.com](https://supabase.com)**

2. **Получите данные для подключения:**
   - Перейдите в Settings → API
   - Скопируйте `Project URL` (это SUPABASE_URL)
   - Скопируйте `anon public` ключ (это SUPABASE_ANON_KEY)
   - Скопируйте `service_role secret` ключ (это SUPABASE_SERVICE_ROLE_KEY)

3. **Получите строку подключения к БД:**
   - Перейдите в Settings → Database
   - В разделе "Connection string" выберите "URI"
   - Скопируйте строку и замените `[YOUR-PASSWORD]` на ваш пароль
   - Это будет DATABASE_URL

4. **Создайте Storage бакеты:**
   - Перейдите в Storage
   - Создайте бакет `tracks` (Private)
   - Создайте бакет `covers` (Public)
   
5. **Настройте политики Storage:**
   
   Для бакета `covers` (публичный доступ на чтение):
   ```sql
   -- Политика для публичного чтения covers
   CREATE POLICY "Public Access" ON storage.objects
     FOR SELECT USING (bucket_id = 'covers');
   ```
   
   Для бакета `tracks` (только через signed URLs):
   ```sql
   -- Нет публичных политик - доступ только через service_role
   ```

### Шаг 2: Настройка Telegram Bot

1. **Создайте бота через [@BotFather](https://t.me/BotFather)**
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните токен бота (TELEGRAM_BOT_TOKEN)

2. **Настройте Web App:**
   - Отправьте `/mybots` → выберите бота
   - Bot Settings → Menu Button → Configure menu button
   - Введите URL вашего приложения (после деплоя)

### Шаг 3: Настройка ЮKassa

1. **Зарегистрируйтесь на [yookassa.ru](https://yookassa.ru)**

2. **После подключения магазина получите:**
   - Shop ID (YOOKASSA_SHOP_ID)
   - Секретный ключ (YOOKASSA_SECRET_KEY)

3. **Настройте Webhooks:**
   - В личном кабинете ЮKassa → Интеграция → HTTP-уведомления
   - Добавьте URL: `https://ваш-домен.vercel.app/api/webhooks/yookassa`
   - Выберите события: `payment.succeeded`, `payment.canceled`, `refund.succeeded`

### Шаг 4: Деплой на Vercel

1. **Форкните или загрузите проект на GitHub**

2. **Создайте проект на [vercel.com](https://vercel.com)**
   - Import Git Repository
   - Выберите репозиторий

3. **Настройте Environment Variables:**

   ```
   # Supabase
   SUPABASE_URL=https://xxx.supabase.co
   SUPABASE_ANON_KEY=eyJ...
   SUPABASE_SERVICE_ROLE_KEY=eyJ...
   
   # Database
   DATABASE_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
   
   # Telegram
   TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
   ADMIN_TELEGRAM_IDS=123456789,987654321
   
   # ЮKassa
   YOOKASSA_SHOP_ID=12345
   YOOKASSA_SECRET_KEY=live_xxx
   
   # App
   VITE_APP_URL=https://your-app.vercel.app
   VITE_SUPABASE_URL=https://xxx.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJ...
   ```

4. **Деплой:**
   - Нажмите Deploy
   - Дождитесь завершения сборки

### Шаг 5: Инициализация базы данных

После первого деплоя выполните миграцию:

```bash
# Локально, с установленными переменными окружения
npx prisma db push
```

Или создайте таблицы вручную в Supabase SQL Editor, используя схему из `prisma/schema.prisma`.

### Шаг 6: Настройка Telegram Bot Web App

1. Вернитесь в @BotFather
2. `/mybots` → выберите бота → Bot Settings → Menu Button
3. Введите URL: `https://your-app.vercel.app`
4. Добавьте кнопку или используйте `/setmenubutton`

---

## 📁 Структура проекта

```
/
├── api/                      # Vercel API Routes
│   ├── _lib/                 # Общие утилиты
│   │   ├── auth.ts           # Авторизация
│   │   ├── prisma.ts         # Prisma клиент
│   │   ├── supabase.ts       # Supabase клиент
│   │   ├── telegram.ts       # Валидация Telegram
│   │   ├── yookassa.ts       # Интеграция ЮKassa
│   │   ├── settings.ts       # Настройки приложения
│   │   └── playLimits.ts     # Система лимитов
│   ├── admin/                # Админские эндпоинты
│   ├── user/                 # Пользовательские эндпоинты
│   ├── tracks/               # Треки
│   ├── categories/           # Категории
│   ├── payments/             # Платежи
│   ├── webhooks/             # Вебхуки
│   ├── play/                 # Запись прослушиваний
│   ├── can-play/             # Проверка лимитов
│   └── stream/               # Генерация signed URLs
│
├── src/                      # Frontend
│   ├── components/           # React компоненты
│   │   ├── ui/               # Базовые UI компоненты
│   │   ├── player/           # Аудиоплеер
│   │   ├── tracks/           # Компоненты треков
│   │   └── layout/           # Layouts
│   ├── pages/
│   │   ├── app/              # Пользовательские страницы
│   │   └── admin/            # Админ-панель
│   ├── lib/
│   │   ├── api/              # API клиент
│   │   ├── hooks/            # React хуки
│   │   └── utils/            # Утилиты
│   └── stores/               # Zustand stores
│
├── prisma/
│   └── schema.prisma         # Схема БД
│
├── public/                   # Статические файлы
└── ...config files
```

---

## 🔧 Локальная разработка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repo-url>
   cd music-streaming-tma
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Создайте `.env` файл:**
   ```bash
   cp .env.example .env
   # Заполните переменные
   ```

4. **Сгенерируйте Prisma клиент:**
   ```bash
   npx prisma generate
   ```

5. **Запустите dev-сервер:**
   ```bash
   npm run dev
   ```

6. **Для тестирования Telegram Web App используйте ngrok:**
   ```bash
   ngrok http 3000
   # Используйте полученный HTTPS URL в @BotFather
   ```

---

## 📊 API Endpoints

### Публичные

| Method | Endpoint | Описание |
|--------|----------|----------|
| GET | `/api/tracks` | Список треков |
| GET | `/api/tracks/:id` | Детали трека |
| GET | `/api/categories` | Список категорий |
| GET | `/api/user/profile` | Профиль пользователя |
| GET | `/api/user/purchases` | Покупки пользователя |
| GET | `/api/user/subscription` | Статус подписки |
| GET | `/api/user/history` | История прослушиваний |
| GET | `/api/can-play/:trackId` | Проверка возможности воспроизведения |
| POST | `/api/play/:trackId` | Запись прослушивания |
| GET | `/api/stream/:trackId` | Получение signed URL |
| POST | `/api/payments/create` | Создание платежа |
| GET | `/api/payments/:id/status` | Статус платежа |

### Административные (требуют isAdmin)

| Method | Endpoint | Описание |
|--------|----------|----------|
| GET/POST | `/api/admin/tracks` | Управление треками |
| GET/PUT/DELETE | `/api/admin/tracks/:id` | Операции с треком |
| GET/POST | `/api/admin/categories` | Управление категориями |
| GET/PUT/DELETE | `/api/admin/categories/:id` | Операции с категорией |
| GET | `/api/admin/users` | Список пользователей |
| GET | `/api/admin/users/:id` | Детали пользователя |
| GET | `/api/admin/payments` | Список платежей |
| GET | `/api/admin/stats/overview` | Общая статистика |
| GET | `/api/admin/stats/tracks` | Статистика треков |
| GET | `/api/admin/stats/charts` | Данные для графиков |
| GET/PUT | `/api/admin/settings` | Настройки приложения |
| POST | `/api/admin/upload-url` | Получение URL для загрузки |

---

## ⚙️ Переменные окружения

| Переменная | Описание | Где взять |
|------------|----------|-----------|
| `SUPABASE_URL` | URL проекта Supabase | Supabase → Settings → API |
| `SUPABASE_ANON_KEY` | Публичный ключ | Supabase → Settings → API |
| `SUPABASE_SERVICE_ROLE_KEY` | Сервисный ключ | Supabase → Settings → API |
| `DATABASE_URL` | Строка подключения к БД | Supabase → Settings → Database |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | @BotFather |
| `ADMIN_TELEGRAM_IDS` | ID админов (через запятую) | @userinfobot или в Telegram |
| `YOOKASSA_SHOP_ID` | ID магазина ЮKassa | Личный кабинет ЮKassa |
| `YOOKASSA_SECRET_KEY` | Секретный ключ ЮKassa | Личный кабинет ЮKassa |
| `VITE_APP_URL` | URL приложения | После деплоя на Vercel |
| `VITE_SUPABASE_URL` | URL Supabase для фронтенда | = SUPABASE_URL |
| `VITE_SUPABASE_ANON_KEY` | Публичный ключ для фронтенда | = SUPABASE_ANON_KEY |

---

## 🔒 Безопасность

- ✅ Валидация Telegram initData (HMAC-SHA256)
- ✅ Проверка timestamp для защиты от replay-атак
- ✅ Аудиофайлы в приватном бакете
- ✅ Доступ к аудио только через signed URLs
- ✅ Проверка прав для админских эндпоинтов
- ✅ SQL injection защита (Prisma ORM)
- ✅ Валидация входных данных

---

## 📝 Примечания

### Лимиты бесплатных планов:

**Vercel Hobby:**
- 100 GB bandwidth/месяц
- 1M function invocations/месяц
- 10s timeout (настроено 30s)

**Supabase Free:**
- 500 MB database
- 1 GB storage
- 2 GB bandwidth/месяц

### Рекомендации:
- Средний MP3 (192kbps, 4 мин) ≈ 6 MB
- С 1 GB storage можно хранить ~150 треков с обложками
- Для масштабирования рассмотрите платные планы

---

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи в Vercel Dashboard
2. Проверьте логи в Supabase Dashboard
3. Убедитесь, что все переменные окружения установлены
4. Проверьте настройки Webhook в ЮKassa

---

## 📄 Лицензия

MIT License
#   a r t i s t  
 